#include <stdio.h>
/**
 * main - prints out the program name followed by a new line.
 * @argc: the argument given to the program.
 * @argv: an array on the number of arguments
 * return: returns 0
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return(0);
}
